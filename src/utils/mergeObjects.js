
export default function mergeObjects(objects, finalCategory) {
  // взять из массива объекты

  // преобразовать их в массивы точек-ребер:
  let str = 'Функция работает';
  let begin = 'Начинаем разработку';
} 